//
//  DesignFonts.swift
//  SahamIndo
//
//  Created by Surya on 18/06/26.
//

import SwiftUI

enum DesignFont {
    
    // MARK: - Large Title
    static let largeTitle: Font = .system(.largeTitle, weight: .regular)
    static let largeTitleMedium: Font = .system(.largeTitle)
    static let largeTitleBold: Font = .system(.largeTitle, weight: .bold)

    // MARK: - Title
    static let title: Font = .system(.title, weight: .regular)
    static let titleMedium: Font = .system(.title)
    static let titleBold: Font = .system(.title, weight: .bold)

    // MARK: - Title2
    static let title2: Font = .system(.title2, weight: .regular)
    static let title2Medium: Font = .system(.title2)
    static let title2Bold: Font = .system(.title2, weight: .bold)

    // MARK: - Title3
    static let title3: Font = .system(.title3, weight: .regular)
    static let title3Medium: Font = .system(.title3)
    static let title3Bold: Font = .system(.title3, weight: .bold)

    // MARK: - Headline
    static let headline: Font = .system(.headline, weight: .regular)
    static let headlineMedium: Font = .system(.headline)
    static let headlineBold: Font = .system(.headline, weight: .bold)

    // MARK: - Subheadline
    static let subheadline: Font = .system(.subheadline, weight: .regular)
    static let subheadlineMedium: Font = .system(.subheadline)
    static let subheadlineBold: Font = .system(.subheadline, weight: .bold)

    // MARK: - Body
    static let body: Font = .system(.body, weight: .regular)
    static let bodyMedium: Font = .system(.body)
    static let bodyBold: Font = .system(.body, weight: .bold)

    // MARK: - Callout
    static let callout: Font = .system(.callout, weight: .regular)
    static let calloutMedium: Font = .system(.callout)
    static let calloutBold: Font = .system(.callout, weight: .bold)

    // MARK: - Footnote
    static let footnote: Font = .system(.footnote, weight: .regular)
    static let footnoteMedium: Font = .system(.footnote)
    static let footnoteBold: Font = .system(.footnote, weight: .bold)

    // MARK: - Caption
    static let caption: Font = .system(.caption, weight: .regular)
    static let captionMedium: Font = .system(.caption)
    static let captionBold: Font = .system(.caption, weight: .bold)

    // MARK: - Caption2
    static let caption2: Font = .system(.caption2, weight: .regular)
    static let caption2Medium: Font = .system(.caption2)
    static let caption2Bold: Font = .system(.caption2, weight: .bold)
}
